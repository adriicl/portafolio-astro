---
const formAction = "#"; // Cambia esto a tu backend o servicio de formulario
---

<section id="contact" class="relative w-full py-20 px-6 text-white overflow-hidden scroll-mt-24 md:scroll-mt-28">
  <div class="max-w-6xl mx-auto">
    
    <!-- Header con título y línea decorativa -->
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold text-green-400 mb-4">Contacto</h2>
      <div class="w-24 h-1 bg-gradient-to-r from-green-400 to-blue-500 mx-auto rounded-full mb-6"></div>
      <p class="text-lg text-gray-300 max-w-2xl mx-auto leading-relaxed">
        ¿Tienes un proyecto, una idea o una consulta? Escríbeme y te responderé lo antes posible.
      </p>
    </div>

    <!-- Contenido principal -->
    <div class="grid lg:grid-cols-2 gap-12 items-start">
      
      <!-- Información de contacto -->
      <div class="space-y-8">
        <div>
          <h3 class="text-2xl font-bold text-white mb-6">Hablemos de tu proyecto</h3>
          <p class="text-lg text-gray-300 leading-relaxed mb-8">
            Estoy siempre abierto a discutir nuevos proyectos, ideas creativas o oportunidades para ser parte de tus visiones.
          </p>
        </div>

        <!-- Tarjetas de información -->
        <div class="space-y-6">
          <div class="flex items-center space-x-4 p-4 bg-gradient-to-r from-green-400/10 to-transparent rounded-lg border-l-4 border-green-400">
            <div class="w-10 h-10 bg-green-400/20 rounded-full flex items-center justify-center">
              <svg class="w-5 h-5 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
              </svg>
            </div>
            <div>
              <h4 class="font-semibold text-white">Email</h4>
              <p class="text-sm text-gray-400">adriancabreraleonis172003@gmail.com</p>
            </div>
          </div>

          <div class="flex items-center space-x-4 p-4 bg-gradient-to-r from-blue-500/10 to-transparent rounded-lg border-l-4 border-blue-400">
            <div class="w-10 h-10 bg-blue-500/20 rounded-full flex items-center justify-center">
              <svg class="w-5 h-5 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
            </div>
            <div>
              <h4 class="font-semibold text-white">Tiempo de respuesta</h4>
              <p class="text-sm text-gray-400">Menos de 24 horas</p>
            </div>
          </div>

          <div class="flex items-center space-x-4 p-4 bg-gradient-to-r from-purple-500/10 to-transparent rounded-lg border-l-4 border-purple-400">
            <div class="w-10 h-10 bg-purple-500/20 rounded-full flex items-center justify-center">
              <svg class="w-5 h-5 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
              </svg>
            </div>
            <div>
              <h4 class="font-semibold text-white">Ubicación</h4>
              <p class="text-sm text-gray-400">Orihuela/Alicante, Remoto</p>
            </div>
          </div>
        </div>

        <!-- CTA adicional -->
        <div class="pt-6">
          <a href="/CV.pdf" target="_blank" class="inline-flex items-center space-x-2 border border-green-400 text-green-400 hover:bg-green-400 hover:text-[#0a192f] px-6 py-3 font-medium rounded-lg transition-all duration-300">
            <span>Descargar CV</span>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v12m0 0l-4-4m4 4l4-4M4 20h16"></path>
            </svg>
          </a>
        </div>
      </div>

      <!-- Formulario -->
      <div class="relative">

        <!-- Fondos decorativos -->
        <div class="pointer-events-none absolute inset-0 -z-10">
          <div class="absolute -top-24 -left-24 w-[420px] h-[420px] bg-green-500/10 rounded-full blur-3xl"></div>
          <div class="absolute -bottom-24 -right-24 w-[420px] h-[420px] bg-blue-500/10 rounded-full blur-3xl"></div>
          <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,_rgba(255,255,255,0.05),_transparent_60%)]"></div>
        </div>

        <!-- Tarjeta del formulario -->
        <form action={formAction} method="POST" class="w-full relative">
          <div class="absolute -inset-0.5 rounded-2xl bg-gradient-to-tr from-green-500/20 to-blue-500/20 blur opacity-70"></div>
          <div class="relative rounded-2xl bg-[#0b1730]/80 backdrop-blur-md border border-gray-700/50 p-8 shadow-2xl">
            
            <!-- Nombre y apellidos -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
              <div>
                <label for="name" class="block text-sm font-medium text-green-400 mb-2">Nombre</label>
                <input
                  type="text"
                  id="name"
                  name="name"
                  required
                  aria-required="true"
                  placeholder="Tu nombre"
                  class="w-full px-4 py-3 bg-gray-900/60 border border-gray-700/50 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-green-400 transition-all duration-300"
                />
              </div>
              <div>
                <label for="surname" class="block text-sm font-medium text-green-400 mb-2">Apellidos</label>
                <input
                  type="text"
                  id="surname"
                  name="surname"
                  required
                  aria-required="true"
                  placeholder="Tus apellidos"
                  class="w-full px-4 py-3 bg-gray-900/60 border border-gray-700/50 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-green-400 transition-all duration-300"
                />
              </div>
            </div>

            <!-- Email y teléfono -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-5 mt-5">
              <div>
                <label for="email" class="block text-sm font-medium text-green-400 mb-2">Email</label>
                <input
                  type="email"
                  id="email"
                  name="email"
                  required
                  aria-required="true"
                  placeholder="tu@email.com"
                  class="w-full px-4 py-3 bg-gray-900/60 border border-gray-700/50 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-green-400 transition-all duration-300"
                />
              </div>
              <div>
                <label for="phone" class="block text-sm font-medium text-green-400 mb-2">Teléfono</label>
                <input
                  type="tel"
                  id="phone"
                  name="phone"
                  placeholder="Opcional"
                  class="w-full px-4 py-3 bg-gray-900/60 border border-gray-700/50 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-green-400 transition-all duration-300"
                />
              </div>
            </div>

            <!-- Empresa -->
            <div class="mt-5">
              <label for="company" class="block text-sm font-medium text-green-400 mb-2">Empresa</label>
              <input
                type="text"
                id="company"
                name="company"
                placeholder="Opcional"
                class="w-full px-4 py-3 bg-gray-900/60 border border-gray-700/50 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-green-400 transition-all duration-300"
              />
            </div>

            <!-- Mensaje -->
            <div class="mt-5">
              <div class="flex items-center justify-between mb-2">
                <label for="message" class="block text-sm font-medium text-green-400">Mensaje</label>
                <span class="text-xs text-gray-400">Máx. 1000 caracteres</span>
              </div>
              <textarea
                id="message"
                name="message"
                rows="5"
                required
                aria-required="true"
                placeholder="Cuéntame sobre tu proyecto, objetivos y plazos..."
                class="w-full px-4 py-3 bg-gray-900/60 border border-gray-700/50 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-green-400 transition-all duration-300 resize-none"
              ></textarea>
            </div>

            <!-- Footer del formulario -->
            <div class="mt-8 flex flex-col md:flex-row items-start md:items-center justify-between gap-4">
              <div class="text-xs text-gray-400">
                Al enviar aceptas nuestra
                <a href="/privacy" class="text-green-400 hover:text-green-300 underline-offset-2 hover:underline transition-colors duration-300">política de privacidad</a>.
              </div>
              <button
                type="submit"
                class="inline-flex items-center justify-center bg-gradient-to-r from-green-400 to-blue-500 hover:from-green-500 hover:to-blue-600 text-[#0a192f] font-semibold py-3 px-8 rounded-lg transition-all duration-300 transform hover:scale-[1.02] shadow-lg hover:shadow-green-500/25"
              >
                Enviar mensaje
                <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
                </svg>
              </button>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</section>
