---
const formAction = "#"; // Cambia esto a tu backend o servicio de formulario
---

<section class="w-full py-16 px-6 text-white flex flex-col items-center">
  <h2 class="text-3xl font-bold text-green-400 text-center mb-8">Contact Me</h2>
  <p class="text-lg text-gray-300 text-center max-w-2xl mb-6">
    Feel free to reach out! Whether you have a project, a question, or just want to say hello, I'll be happy to connect with you.
  </p>

  <form action={formAction} method="POST" class="w-full max-w-2xl bg-[#0a192f] p-8 rounded-lg shadow-lg border border-green-400">
    
    <!-- Nombre y Apellidos -->
    <div class="flex flex-col md:flex-row gap-4">
      <div class="w-full">
        <label for="name" class="block text-sm font-medium text-green-400">First Name</label>
        <input type="text" id="name" name="name" required class="mt-1 block w-full px-4 py-2 bg-gray-800 border border-gray-600 rounded-md text-white focus:outline-none focus:ring-2 focus:ring-green-400">
      </div>
      <div class="w-full">
        <label for="surname" class="block text-sm font-medium text-green-400">Last Name</label>
        <input type="text" id="surname" name="surname" required class="mt-1 block w-full px-4 py-2 bg-gray-800 border border-gray-600 rounded-md text-white focus:outline-none focus:ring-2 focus:ring-green-400">
      </div>
    </div>

    <!-- Email y Número de Teléfono -->
    <div class="flex flex-col md:flex-row gap-4 mt-4">
      <div class="w-full">
        <label for="email" class="block text-sm font-medium text-green-400">Email</label>
        <input type="email" id="email" name="email" required class="mt-1 block w-full px-4 py-2 bg-gray-800 border border-gray-600 rounded-md text-white focus:outline-none focus:ring-2 focus:ring-green-400">
      </div>
      <div class="w-full">
        <label for="phone" class="block text-sm font-medium text-green-400">Phone Number</label>
        <input type="tel" id="phone" name="phone" required class="mt-1 block w-full px-4 py-2 bg-gray-800 border border-gray-600 rounded-md text-white focus:outline-none focus:ring-2 focus:ring-green-400">
      </div>
    </div>

    <!-- Nombre de la empresa -->
    <div class="mt-4">
      <label for="company" class="block text-sm font-medium text-green-400">Company Name</label>
      <input type="text" id="company" name="company" class="mt-1 block w-full px-4 py-2 bg-gray-800 border border-gray-600 rounded-md text-white focus:outline-none focus:ring-2 focus:ring-green-400">
    </div>

    <!-- Mensaje -->
    <div class="mt-4">
      <label for="message" class="block text-sm font-medium text-green-400">Message</label>
      <textarea id="message" name="message" rows="4" required class="mt-1 block w-full px-4 py-2 bg-gray-800 border border-gray-600 rounded-md text-white focus:outline-none focus:ring-2 focus:ring-green-400"></textarea>
    </div>

    <!-- Botón de envío -->
    <div class="mt-6 text-center">
      <button type="submit" class="w-full bg-green-400 text-black py-3 px-6 rounded-md font-semibold text-lg transition hover:bg-green-500">
        Send Message
      </button>
    </div>
  </form>
</section>
