---
const formAction = "#"; // Cambia esto a tu backend o servicio de formulario
---

<section class="relative w-full py-20 px-6 text-white flex flex-col items-center overflow-hidden">
  <!-- Fondos decorativos suaves -->
  <div class="pointer-events-none absolute inset-0 -z-10">
    <div class="absolute -top-24 -left-24 w-[420px] h-[420px] bg-green-500/10 rounded-full blur-3xl"></div>
    <div class="absolute -bottom-24 -right-24 w-[420px] h-[420px] bg-blue-500/10 rounded-full blur-3xl"></div>
    <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,_rgba(255,255,255,0.05),_transparent_60%)]"></div>
  </div>

  <!-- Encabezado -->
  <div class="text-center mb-10">
    <h2 class="text-4xl font-bold text-green-400">Contacto</h2>
    <div class="w-24 h-1 bg-gradient-to-r from-green-400 to-blue-500 mx-auto rounded-full mt-3"></div>
    <p class="text-gray-300 max-w-2xl mt-4">
      ¿Tienes un proyecto, una idea o una consulta? Escríbeme y te responderé lo antes posible.
    </p>
  </div>

  <!-- Tarjeta del formulario -->
  <form action={formAction} method="POST" class="w-full max-w-3xl relative">
    <div class="absolute -inset-0.5 rounded-2xl bg-gradient-to-tr from-green-500/20 to-blue-500/20 blur opacity-70"></div>
    <div class="relative rounded-2xl bg-[#0b1730]/80 backdrop-blur-md border border-gray-700/50 p-8 shadow-2xl">
      
      <!-- Nombre y apellidos -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
        <div>
          <label for="name" class="block text-sm font-medium text-green-400">Nombre</label>
          <input
            type="text"
            id="name"
            name="name"
            required
            aria-required="true"
            placeholder="Tu nombre"
            class="mt-1 block w-full px-4 py-3 bg-gray-900/60 border border-gray-700/50 rounded-md text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-green-400 transition"
          />
        </div>
        <div>
          <label for="surname" class="block text-sm font-medium text-green-400">Apellidos</label>
          <input
            type="text"
            id="surname"
            name="surname"
            required
            aria-required="true"
            placeholder="Tus apellidos"
            class="mt-1 block w-full px-4 py-3 bg-gray-900/60 border border-gray-700/50 rounded-md text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-green-400 transition"
          />
        </div>
      </div>

      <!-- Email y teléfono -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-5 mt-5">
        <div>
          <label for="email" class="block text-sm font-medium text-green-400">Email</label>
          <input
            type="email"
            id="email"
            name="email"
            required
            aria-required="true"
            placeholder="tu@email.com"
            class="mt-1 block w-full px-4 py-3 bg-gray-900/60 border border-gray-700/50 rounded-md text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-green-400 transition"
          />
        </div>
        <div>
          <label for="phone" class="block text-sm font-medium text-green-400">Teléfono</label>
          <input
            type="tel"
            id="phone"
            name="phone"
            placeholder="Opcional"
            class="mt-1 block w-full px-4 py-3 bg-gray-900/60 border border-gray-700/50 rounded-md text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-green-400 transition"
          />
        </div>
      </div>

      <!-- Empresa -->
      <div class="mt-5">
        <label for="company" class="block text-sm font-medium text-green-400">Empresa</label>
        <input
          type="text"
          id="company"
          name="company"
          placeholder="Opcional"
          class="mt-1 block w-full px-4 py-3 bg-gray-900/60 border border-gray-700/50 rounded-md text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-green-400 transition"
        />
      </div>

      <!-- Mensaje -->
      <div class="mt-5">
        <div class="flex items-center justify-between">
          <label for="message" class="block text-sm font-medium text-green-400">Mensaje</label>
          <span class="text-xs text-gray-400">Máx. 1000 caracteres</span>
        </div>
        <textarea
          id="message"
          name="message"
          rows="5"
          required
          aria-required="true"
          placeholder="Cuéntame sobre tu proyecto, objetivos y plazos..."
          class="mt-1 block w-full px-4 py-3 bg-gray-900/60 border border-gray-700/50 rounded-md text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-green-400 transition"
        ></textarea>
      </div>

      <!-- Footer del formulario -->
      <div class="mt-6 flex flex-col md:flex-row items-start md:items-center justify-between gap-4">
        <div class="text-xs text-gray-400">
          Al enviar aceptas nuestra
          <a href="/privacy" class="text-green-400 hover:text-green-300 underline-offset-2 hover:underline">política de privacidad</a>.
        </div>
        <button
          type="submit"
          class="inline-flex items-center justify-center bg-gradient-to-r from-green-400 to-blue-500 hover:from-green-500 hover:to-blue-600 text-[#0a192f] font-semibold py-3 px-6 rounded-md transition transform hover:scale-[1.02] shadow-lg"
        >
          Enviar mensaje
          <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
          </svg>
        </button>
      </div>
    </div>
  </form>
</section>
